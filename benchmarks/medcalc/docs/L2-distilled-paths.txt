========== SECRETAGENT ==========

Path A: Python regex extraction + Python computation. NO LLM CALL.
  calculators.calculate(patient_note, question)
  -> identify_calculator(question) matches 'body mass index'
  -> extract_weight_kg(text) -> 80.0
  -> extract_height_cm(text) -> 175.0
  -> bmi = 80 / (1.75)^2 = 26.122

Path B: Python fails -> simulate_medical_value(patient_note, question)
  The LLM sees the EXACT SAME PROMPT as L1 above.


========== AGENTPROJECT ==========

Same structure. @distilled(fallback_ptool=calculate_medical_value)
  Path A: Python regex + calculation (same calculators.py code)
  Path B: Falls back to L1 @ptool — EXACT SAME PROMPT as L1 above.
